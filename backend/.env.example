# Database Configuration (k1 schema - platform database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=workflowpp
DB_USER=postgres
DB_PASSWORD=postgres

# Application Configuration
PORT=5000
NODE_ENV=development
ANTHROPIC_API_KEY=your_api_key_here

# Enable Multi-Agent System (generates forms, data models, and mobile screens)
USE_MULTI_AGENT=true

# Enable Mixture of Experts (most advanced - smart routing)
USE_MOE=true
